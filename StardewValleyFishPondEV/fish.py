from enum import Enum


# Fish we want to run EVs for and their base sell value.
class Fish(Enum):
    PUFFERFISH = 200
    LAVA_EEL = 700
    STURGEON = 200
    ICE_PIP = 500
    STONEFISH = 300
    SUPER_CUCUMBER = 250
